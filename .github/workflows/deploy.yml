name: Deploy to AWS SAM

on:
  push:
    branches:
      - main  # Trigger the workflow on push to the main branch

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '18'

      # Navigate to the backend folder where package.json is located
      - name: Install dependencies
        run: |
          cd backend  # Move to the backend directory where your package.json is located
          npm install

      - name: Install AWS SAM CLI
        run: |
          sudo apt-get install awscli -y
          wget https://github.com/aws/aws-sam-cli/releases/latest/download/aws-sam-cli-linux-x86_64.zip
          unzip aws-sam-cli-linux-x86_64.zip -d sam-installation
          sudo ./sam-installation/install

      - name: Build SAM application
        run: |
          cd backend  # Ensure you're in the right directory before running SAM commands
          sam build

      - name: Deploy SAM application
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: us-east-1
        run: |
          cd backend  # Ensure you're in the right directory before deploying
          sam deploy --no-confirm-changeset --stack-name event-survey-app --capabilities CAPABILITY_IAM
